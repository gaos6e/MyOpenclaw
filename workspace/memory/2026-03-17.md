- 用户明确了“自我提升”机制的严格标准：只有真正动手执行后才算开始；若只是准备、检查或被新任务打断，不得声称已执行。需要明确区分“准备开始 / 已开始执行 / 被中断”。
- 用户要求自我提升机制采用三层结构：self-improving-agent 负责记录；每15分钟一次的“自我提升检查”cron负责执行；每日22:00“每日进化与待办”cron负责汇报。
- 已创建并启用 cron 任务“自我提升检查”（ID: dfb9f773-ba75-47ee-812e-50b16acaab42），频率每15分钟，当前为方案A：偏记录框架+执行器+总结器，不是强制自动写 learnings 的方案B。
- 用户确认 self-improving-agent 的定位：它本身只负责记录与沉淀（错误、纠正、最佳实践、功能需求），不直接负责执行改进。
- 已发现并处理重复 skill：删除了重复目录 self-improving-agent-3.0.4，仅保留 self-improving-agent，避免加载歧义。
- 已完成 capability-evolver 基础注册：通过 EvoMap hello 生成并写入 A2A_NODE_ID=node_64f522bbf427 到 .env，同时设置 A2A_HUB_URL=https://evomap.ai。
- 已初始化 ontology 本地模式：创建 memory/ontology/graph.jsonl 与 schema.yaml，并写入基础 schema；ontology 默认本地可用，不需要密钥，只有接外部服务才需要 secret_ref 对应的密钥引用。
- 已检查 openclaw-workspace 技能配置状态：当前仍不能直接正常使用的主要有 gog（缺 OAuth client_secret.json）、mcporter（无 MCP 服务）、web-scraper / web-scraper-as-a-service（缺目标网址与字段配置）、news-summary 的语音摘要（缺 OPENAI_API_KEY）。
- 已安装/配置可用的能力包括：playwright、playwright-scraper-skill、office-automation 依赖、agent-browser、Tavily API Key、ontology、自我提升 cron 等。
- 更新 USER.md：补全用户时区为 Asia/Shanghai。
